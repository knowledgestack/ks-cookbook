# Real estate

_Generated. Do not edit by hand._

[← Back to flagships book](../flagships.md)

## condo_board_decision_pack

> Source: [`flagships/condo_board_decision_pack`](../../../flagships/condo_board_decision_pack)

### Condo Board Decision Pack

> **Condo board decision pack CLI — outputs board-ready docx.**

#### Table of contents

1. [What this flagship does](#what-this-flagship-does)
2. [How it works](#how-it-works)
3. [Sign in to Knowledge Stack](#sign-in-to-knowledge-stack)
4. [Ingest the unified corpus](#ingest-the-unified-corpus)
5. [Inputs](#inputs)
6. [Output schema](#output-schema)
7. [Run](#run)
8. [Verification status](#verification-status)
9. [Files](#files)

#### What this flagship does

Condo board decision pack CLI — outputs board-ready docx.

#### How it works

1. The flagship spawns the `knowledgestack-mcp` stdio server (auth via `KS_API_KEY`).
2. A pydantic-ai `Agent` (or raw OpenAI tool-calling loop in some flagships) is built with a strict pydantic output schema.
3. The agent asks Knowledge Stack natural-language questions via `search_knowledge` — no folder UUIDs are needed; KS finds the right document by content.
4. For every search hit the agent calls `read(path_part_id=<hit>)` to fetch the chunk text and the `[chunk:<uuid>]` citation marker.
5. The validated output is rendered to a file artifact (`.md` / `.docx` / `.xlsx`) under this folder as `sample_output.<ext>`.

#### Sign in to Knowledge Stack

**Path A — `ingestion: true` (shared cookbook tenant, fastest)**

```bash
export KS_API_KEY=sk-user-...
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

**Path B — `ingestion: false` (clone repo, ingest your own data)**

```bash
git clone https://github.com/knowledgestack/ks-cookbook
cd ks-cookbook
make install
export KS_API_KEY=sk-user-...   # your own KS key
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

#### Ingest the unified corpus

Path B only — one-time. The bundled `seed/` folder has 34 real public-domain documents across 13 verticals.

```bash
make seed-unified-corpus PARENT_FOLDER_ID=<your-folder-uuid>
```

#### Inputs

| Flag | Required | Default | Help |
|---|---|---|---|

| `--request` | yes | — |  |

| `--unit` | no | '4B' |  |

| `--corpus-folder` | no | — |  |

| `--model` | no | — |  |

| `--out` | no | — |  |


**Sample inputs** in `sample_inputs/`:

- `request.md`

#### Output schema

`BoardDecisionPack` (in `schema.py`) — emitted as a structured artifact.

| Field | Type |
|---|---|

| `request_summary` | `str` |

| `unit` | `str` |

| `verdict` | `str` |

| `required_vote_threshold` | `str` |

| `governing_rules` | `list[GoverningRule]` |

| `precedents` | `list[Precedent]` |

| `reserve_impact` | `ReserveImpact | None` |

| `conditions` | `list[str]` |

| `rationale` | `str` |

| `recommended_motion` | `str` |

#### Run

From the repo root:

```bash
make demo-condo-board-decision-pack
```

Or directly:

```bash
uv run --package ks-cookbook-condo-board-decision-pack ks-cookbook-condo-board-decision-pack --help
```

#### Verification status

🚧 **SCHEMA_ERROR** — flagship is currently a known-issue. Likely causes: stale `__CORPUS_FOLDER_ID__` references, raw OpenAI tool-calling not yet refactored to the search→read pattern, or a CLI default that requires manual setup. Tracked in the upcoming flagship sweep.

#### Files

```text
flagships/condo_board_decision_pack/
├── README.md          ← you are here
├── pyproject.toml
├── sample_inputs/     (where applicable)
├── sample_output.<ext> (generated by `make demo-condo-board-decision-pack`)
└── src/condo_board_decision/
    ├── __main__.py    ← CLI entry
    ├── agent.py       ← pydantic-ai Agent + system prompt
    └── schema.py      ← pydantic output schema
```

---

## lease_abstract

> Source: [`flagships/lease_abstract`](../../../flagships/lease_abstract)

### Lease Abstract

> **CLI entry for the lease-abstract demo.**

#### Table of contents

1. [What this flagship does](#what-this-flagship-does)
2. [How it works](#how-it-works)
3. [Sign in to Knowledge Stack](#sign-in-to-knowledge-stack)
4. [Ingest the unified corpus](#ingest-the-unified-corpus)
5. [Inputs](#inputs)
6. [Output schema](#output-schema)
7. [Run](#run)
8. [Verification status](#verification-status)
9. [Files](#files)

#### What this flagship does

CLI entry for the lease-abstract demo.

#### How it works

1. The flagship spawns the `knowledgestack-mcp` stdio server (auth via `KS_API_KEY`).
2. A pydantic-ai `Agent` (or raw OpenAI tool-calling loop in some flagships) is built with a strict pydantic output schema.
3. The agent asks Knowledge Stack natural-language questions via `search_knowledge` — no folder UUIDs are needed; KS finds the right document by content.
4. For every search hit the agent calls `read(path_part_id=<hit>)` to fetch the chunk text and the `[chunk:<uuid>]` citation marker.
5. The validated output is rendered to a file artifact (`.md` / `.docx` / `.xlsx`) under this folder as `sample_output.<ext>`.

#### Sign in to Knowledge Stack

**Path A — `ingestion: true` (shared cookbook tenant, fastest)**

```bash
export KS_API_KEY=sk-user-...
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

**Path B — `ingestion: false` (clone repo, ingest your own data)**

```bash
git clone https://github.com/knowledgestack/ks-cookbook
cd ks-cookbook
make install
export KS_API_KEY=sk-user-...   # your own KS key
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

#### Ingest the unified corpus

Path B only — one-time. The bundled `seed/` folder has 34 real public-domain documents across 13 verticals.

```bash
make seed-unified-corpus PARENT_FOLDER_ID=<your-folder-uuid>
```

#### Inputs

| Flag | Required | Default | Help |
|---|---|---|---|

| `--lease` | no | 'retail_lease_pinetree_crossing' | Name of the lease document inside the corpus folder. |

| `--lease-file` | no | — | File whose first line overrides --lease. |

| `--out` | no | — |  |

| `--corpus-folder` | no | — |  |

| `--model` | no | — |  |


**Sample inputs** in `sample_inputs/`:

- `lease.txt`

#### Output schema

Output is a Markdown / DOCX / XLSX artifact written to this folder.

#### Run

From the repo root:

```bash
make demo-lease-abstract
```

Or directly:

```bash
uv run --package ks-cookbook-lease-abstract ks-cookbook-lease-abstract --help
```

#### Verification status

⚠️ **EMPTY_OUTPUT** — last run produced an artifact but the verifier didn't find `[chunk:<uuid>]` citation markers in the stdout. The flagship may be writing markers into a binary artifact (.docx/.xlsx) where the verifier doesn't currently scan, or the agent skipped grounding. See [`docs/RFC_KS_MCP_HANDHOLDING.md`](../../docs/RFC_KS_MCP_HANDHOLDING.md).

#### Files

```text
flagships/lease_abstract/
├── README.md          ← you are here
├── pyproject.toml
├── sample_inputs/     (where applicable)
├── sample_output.<ext> (generated by `make demo-lease-abstract`)
└── src/lease_abstract/
    ├── __main__.py    ← CLI entry
    ├── agent.py       ← pydantic-ai Agent + system prompt
    └── schema.py      ← pydantic output schema
```

---

## zoning_compliance_check

> Source: [`flagships/zoning_compliance_check`](../../../flagships/zoning_compliance_check)

### Zoning Compliance Check

> **CLI entry for the zoning compliance check demo.**

#### Table of contents

1. [What this flagship does](#what-this-flagship-does)
2. [How it works](#how-it-works)
3. [Sign in to Knowledge Stack](#sign-in-to-knowledge-stack)
4. [Ingest the unified corpus](#ingest-the-unified-corpus)
5. [Inputs](#inputs)
6. [Output schema](#output-schema)
7. [Run](#run)
8. [Verification status](#verification-status)
9. [Files](#files)

#### What this flagship does

CLI entry for the zoning compliance check demo.

#### How it works

1. The flagship spawns the `knowledgestack-mcp` stdio server (auth via `KS_API_KEY`).
2. A pydantic-ai `Agent` (or raw OpenAI tool-calling loop in some flagships) is built with a strict pydantic output schema.
3. The agent asks Knowledge Stack natural-language questions via `search_knowledge` — no folder UUIDs are needed; KS finds the right document by content.
4. For every search hit the agent calls `read(path_part_id=<hit>)` to fetch the chunk text and the `[chunk:<uuid>]` citation marker.
5. The validated output is rendered to a file artifact (`.md` / `.docx` / `.xlsx`) under this folder as `sample_output.<ext>`.

#### Sign in to Knowledge Stack

**Path A — `ingestion: true` (shared cookbook tenant, fastest)**

```bash
export KS_API_KEY=sk-user-...
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

**Path B — `ingestion: false` (clone repo, ingest your own data)**

```bash
git clone https://github.com/knowledgestack/ks-cookbook
cd ks-cookbook
make install
export KS_API_KEY=sk-user-...   # your own KS key
export KS_BASE_URL=https://api.knowledgestack.ai
export OPENAI_API_KEY=sk-...
export MODEL=gpt-4o-mini
```

#### Ingest the unified corpus

Path B only — one-time. The bundled `seed/` folder has 34 real public-domain documents across 13 verticals.

```bash
make seed-unified-corpus PARENT_FOLDER_ID=<your-folder-uuid>
```

#### Inputs

| Flag | Required | Default | Help |
|---|---|---|---|

| `--address` | no | '2100 S Congress Ave, Austin, TX 78704' | Street address of the property. |

| `--zone-district` | no | 'GR' | Zoning district code (default: GR). |

| `--proposed-use` | no | 'outdoor entertainment venue' | Proposed use to check. |

| `--corpus-folder` | no | — | Folder id containing the zoning corpus. |

| `--model` | no | — | OpenAI model (default: gpt-4o). |

| `--out` | no | — | Output markdown file (default: zoning-compliance.md). |


**Sample inputs** in `sample_inputs/`:

- `scenario.txt`

#### Output schema

Output is a Markdown / DOCX / XLSX artifact written to this folder.

#### Run

From the repo root:

```bash
make demo-zoning-compliance-check
```

Or directly:

```bash
uv run --package ks-cookbook-zoning-compliance-check ks-cookbook-zoning-compliance-check --help
```

#### Verification status

⚠️ **EMPTY_OUTPUT** — last run produced an artifact but the verifier didn't find `[chunk:<uuid>]` citation markers in the stdout. The flagship may be writing markers into a binary artifact (.docx/.xlsx) where the verifier doesn't currently scan, or the agent skipped grounding. See [`docs/RFC_KS_MCP_HANDHOLDING.md`](../../docs/RFC_KS_MCP_HANDHOLDING.md).

#### Files

```text
flagships/zoning_compliance_check/
├── README.md          ← you are here
├── pyproject.toml
├── sample_inputs/     (where applicable)
├── sample_output.<ext> (generated by `make demo-zoning-compliance-check`)
└── src/zoning_compliance_check/
    ├── __main__.py    ← CLI entry
    ├── agent.py       ← pydantic-ai Agent + system prompt
    └── schema.py      ← pydantic output schema
```

---

