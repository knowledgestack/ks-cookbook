# clause_extractor

Contract name/keyword → cited inventory of 12 standard clauses (LoL, indemnity,
IP, auto-renew, governing law, …) with present=true/false + excerpts.

```bash
uv run python recipes/clause_extractor/recipe.py --contract "msa verdant"
```
