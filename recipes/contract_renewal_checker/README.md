# contract_renewal_checker

Contract → cited summary: term end, auto-renewal, notice window, fee
escalator, calendar-ready actions.

```bash
uv run python recipes/contract_renewal_checker/recipe.py --contract "msa verdant"
```
