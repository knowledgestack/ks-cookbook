# launch_email_variants

Feature + audience → three cited email variants (value / proof / curiosity)
ready for A/B/C testing.

```bash
uv run python recipes/launch_email_variants/recipe.py --feature "version diff view"
```
