# expense_policy_violation

Expense line → cited verdict (allowed / OOP / needs receipt / needs approval)
against your T&E policy.

```bash
uv run python recipes/expense_policy_violation/recipe.py \
  --description "Client dinner, 6 attendees" --amount "$420" --category meals
```
