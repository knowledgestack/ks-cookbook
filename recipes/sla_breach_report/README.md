# sla_breach_report

Customer + period → cited SLA breach summary with applicable credits and cap
check, per MSA + SLA schedule.

```bash
uv run python recipes/sla_breach_report/recipe.py --customer "Northwind" --period "Q1 2026"
```
