# meddic_call_coach

Call + transcript → per-MEDDIC-field coverage (missing / partial / strong) +
top-3 follow-up questions + overall grade. Cited to playbook + transcript.

```bash
uv run python recipes/meddic_call_coach/recipe.py \
  --call-name "Acme discovery 04-18" --transcript-file call.txt
```
