# tech_debt_ledger

Area keyword → prioritized (impact × effort) tech-debt list cited from retros,
post-mortems, design docs, and ADRs.

```bash
uv run python recipes/tech_debt_ledger/recipe.py --area "ingestion pipeline"
```
