# well_log_summarizer

Well ID → cited HSE + equipment + formation event summary from drilling
reports and well logs. Default corpus: DOE OpenEnergy + state RRC filings.

```bash
uv run python recipes/well_log_summarizer/recipe.py --well-id "42-123-45678"
```
