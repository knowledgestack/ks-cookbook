# obligation_tracker

Contract → cited flat list of obligations (what, owner, counterparty, due,
penalty). Load the JSON into any tracker.

```bash
uv run python recipes/obligation_tracker/recipe.py --contract "msa verdant"
```
