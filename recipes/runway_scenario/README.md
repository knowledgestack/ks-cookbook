# runway_scenario

Scenario (baseline / downside / upside) → cited runway table with driver-level
assumptions sourced from plan + actuals.

```bash
uv run python recipes/runway_scenario/recipe.py --scenario downside
```
