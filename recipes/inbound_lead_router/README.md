# inbound_lead_router

Lead form payload → cited segment + owner team + reply SLA + next action.

```bash
uv run python recipes/inbound_lead_router/recipe.py \
  --payload '{"company":"Acme","size":"250","use_case":"policy RAG"}'
```
