# pip_drafter

Employee + concerns → cited PIP: SMART goals, check-in cadence, manager
support — grounded in your HR performance policy.

```bash
uv run python recipes/pip_drafter/recipe.py \
  --employee "Alex Kim" --concerns "missed sprint commitments last two quarters"
```
