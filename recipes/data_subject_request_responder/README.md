# data_subject_request_responder

GDPR/CCPA DSR intake → cited response plan (systems to query, deadline, policy
refs) + draft response.

```bash
uv run python recipes/data_subject_request_responder/recipe.py \
  --type erasure --jurisdiction GDPR
```
