# case_study_drafter

Customer → cited case-study draft (hero stat, challenge, solution, results,
pull quote) from QBRs + call notes + success plans.

```bash
uv run python recipes/case_study_drafter/recipe.py --customer "Northwind Foods"
```
