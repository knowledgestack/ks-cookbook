# sre_runbook_gap_check

Service → cited gap report: section-by-section (alerts, triage, escalation,
rollback, …) with present/quality + top fixes to close gaps.

```bash
uv run python recipes/sre_runbook_gap_check/recipe.py --service ingest-worker
```
