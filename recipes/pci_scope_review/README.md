# pci_scope_review

System → cited PCI DSS v4 scope classification (in-scope / connected-to /
out-of-scope), applicable controls, compensating controls.

```bash
uv run python recipes/pci_scope_review/recipe.py --system "billing-worker"
```
