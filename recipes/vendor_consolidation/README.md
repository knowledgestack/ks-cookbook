# vendor_consolidation

Category → cited vendor-by-vendor action plan (keep / migrate / terminate /
renegotiate) + savings estimate.

```bash
uv run python recipes/vendor_consolidation/recipe.py --category observability
```
