# stakeholder_map_drafter

Account → cited stakeholder map (role + sentiment + last-touch) + gaps (missing
role coverage).

```bash
uv run python recipes/stakeholder_map_drafter/recipe.py --account "Northwind"
```
