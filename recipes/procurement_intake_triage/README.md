# procurement_intake_triage

Request summary → cited triage: category, data classification, per-reviewer
steps (with SLA days), total-days estimate.

```bash
uv run python recipes/procurement_intake_triage/recipe.py \
  --request "New AI notetaker SaaS for sales calls, 50 users"
```
