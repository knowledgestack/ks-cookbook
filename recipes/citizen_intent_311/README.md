# citizen_intent_311

311 message → cited intent category + department + urgency + SLA + follow-up
template.

```bash
uv run python recipes/citizen_intent_311/recipe.py \
  --message "Large pothole on Elm St near 3rd Ave"
```
