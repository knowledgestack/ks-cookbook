# cloud_cost_anomaly

AWS CUR extract → cited anomaly explanations (service, usage type, delta,
likely cause, recommended action) paired with deploy notes + runbooks.

```bash
uv run python recipes/cloud_cost_anomaly/recipe.py --cur-file march-cur.csv
```
