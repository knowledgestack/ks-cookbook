# resume_jd_scorer

Role + resume → cited per-criterion fit score (must-have / nice-to-have) with
an explicit list of non-job factors ignored for bias reduction.

```bash
uv run python recipes/resume_jd_scorer/recipe.py --role "Staff SWE" --resume-file resume.txt
```
