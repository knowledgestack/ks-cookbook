# fnol_structurer

Free-text FNOL (call transcript or email) → structured claim JSON with policy
#, insured, parties, loss type, and open coverage questions cited to policy.

```bash
uv run python recipes/fnol_structurer/recipe.py --fnol-file fnol-call-042.txt
```
