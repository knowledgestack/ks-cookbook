# coa_mapper

Source COA CSV + source/target system → cited mapping table with confidence
and needs-human-review flag.

```bash
uv run python recipes/coa_mapper/recipe.py --csv qb-accounts.csv --source QuickBooks --target NetSuite
```
