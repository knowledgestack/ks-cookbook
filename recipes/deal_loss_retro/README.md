# deal_loss_retro

Lost deal → cited root-cause retro (separating stated from actual) + playbook
gap list.

```bash
uv run python recipes/deal_loss_retro/recipe.py --deal "Acme Corp Q4 expansion"
```
