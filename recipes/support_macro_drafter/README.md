# support_macro_drafter

Ticket type → cited macro: greeting, body, when/when-not-to-use, escalation
path, grounded in your KB.

```bash
uv run python recipes/support_macro_drafter/recipe.py --ticket-type "API key rotation"
```
