# account_research_brief

AE pre-call prep: account name → cited brief with recent activity, objections,
product fit, and recommended next step.

```bash
uv run python recipes/account_research_brief/recipe.py --account "Verdant Sourcing"
```

Set `ACCOUNT_NOTES_FOLDER_ID` to point at your call-notes folder.
