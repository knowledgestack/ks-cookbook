# icd10_coder

De-identified clinical note → primary + secondary ICD-10-CM code suggestions
with confidence and citation into the ICD-10 reference.

```bash
uv run python recipes/icd10_coder/recipe.py --note-file deid-visit.txt
```
